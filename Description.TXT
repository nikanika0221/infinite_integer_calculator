This Project Takes input from a text file and makes operations, the main point of this project is that
the program uses arrays to save every digit, so the number can have as many digits as array can hold.
(0-9) is held at any index of array. 
